services:
  - type: web
    name: gerontocracy-app
    env: python
    buildCommand: python -m pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && python main.py
    startCommand: shiny run --host 0.0.0.0 --port $PORT app.py